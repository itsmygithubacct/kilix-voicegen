{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:kilix:voicegen:schema:pronunciation-lexicon-header:v1",
  "title": "Kilix Voicegen pronunciation lexicon JSONL header",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema", "admission", "dialect", "resource_id", "segment_inventory_sha256", "review_record_sha256", "entry_count"],
  "properties": {
    "schema": {"const": "kilix.voicegen.pronunciation-lexicon/v1"},
    "admission": {"enum": ["product-admitted", "local-user", "test-fixture"]},
    "dialect": {"const": "en-AU"},
    "resource_id": {"type": "string", "pattern": "^[a-z][a-z0-9._:/-]{0,127}$"},
    "segment_inventory_sha256": {"$ref": "#/$defs/sha256"},
    "review_record_sha256": {
      "oneOf": [
        {"type": "null"},
        {"$ref": "#/$defs/sha256"}
      ]
    },
    "entry_count": {"type": "integer", "minimum": 0, "maximum": 200000}
  },
  "allOf": [
    {
      "if": {"properties": {"admission": {"const": "product-admitted"}}},
      "then": {
        "properties": {
          "entry_count": {"minimum": 1},
          "review_record_sha256": {"$ref": "#/$defs/sha256"}
        }
      },
      "else": {"properties": {"review_record_sha256": {"type": "null"}}}
    }
  ],
  "$defs": {
    "sha256": {"type": "string", "pattern": "^[0-9a-f]{64}$"}
  }
}
